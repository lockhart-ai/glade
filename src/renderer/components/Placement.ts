/** Where an overlay sits relative to the element it's anchored to. It flips or shifts when there isn't room. */
export enum Placement {
  Top = 'top',
  TopStart = 'top-start',
  TopEnd = 'top-end',
  Bottom = 'bottom',
  BottomStart = 'bottom-start',
  BottomEnd = 'bottom-end',
}
