{
  "name": "glade",
  "version": "0.0.0",
  "private": true,
  "description": "A calm macOS app for running Claude agent sessions as tasks.",
  "license": "MIT",
  "main": "./out/main/index.js",
  "scripts": {
    "dev": "electron-vite dev --watch",
    "build": "electron-vite build",
    "start": "electron-vite preview",
    "typecheck": "tsc --build",
    "lint": "echo 'lint is not set up yet: see P0-02 (#3)' && exit 1",
    "format": "echo 'format is not set up yet: see P0-02 (#3)' && exit 1",
    "test": "echo 'test is not set up yet: see P0-03 (#4)' && exit 1"
  },
  "dependencies": {
    "react": "^19.3.0",
    "react-dom": "^19.3.0"
  },
  "devDependencies": {
    "@types/node": "^24.13.6",
    "@types/react": "^19.3.0",
    "@types/react-dom": "^19.3.0",
    "@vitejs/plugin-react": "^5.2.0",
    "electron": "^44.4.5",
    "electron-vite": "^5.0.0",
    "typescript": "~6.0.3",
    "vite": "^7.3.6"
  }
}
